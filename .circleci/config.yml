version: 2
jobs:
  test:
    docker:
      - image: node:10.17.0-alpine
    steps:
      - checkout
      - run:
          name: "run test"
          command: "npm test"
workflows:
  version: 2
  runTest:
    jobs:
      - test
